export type Book = {
    id: string
    title: string
    author: string
    date: string
    ganre: string
}

export type Word = {
    bookId: string
    id: string
    word: string
    read: string
    pageNum: string
    example: string
}